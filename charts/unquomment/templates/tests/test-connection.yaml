apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unquomment.fullname" . }}-test-connection"
  labels:
    {{- include "unquomment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unquomment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

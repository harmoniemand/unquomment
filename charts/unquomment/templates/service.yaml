apiVersion: v1
kind: Service
metadata:
  name: {{ include "unquomment.fullname" . }}
  labels:
    {{- include "unquomment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unquomment.selectorLabels" . | nindent 4 }}
